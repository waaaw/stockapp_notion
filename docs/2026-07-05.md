# 2026-07-05 작업 기록

## 오늘 한 일

### 1. 웹 UI에 "보유 현황" 표 추가
- 사용자가 웹 UI에서 보유 수량/수익률을 전혀 확인할 수 없다는 문제 제기
- `portfolio.list_portfolio_summary()`가 포트폴리오 요약 DB + 종목마스터를 조인해 반환하도록 하고,
  웹 UI 상단에 종목별 보유수량/평균단가/평가금액/평가손익/수익률 + 전체 합계 표 추가
- 실제 워크스페이스로 재기동해 삼성전자 10주, 평가손익 +2,395,000원(+342.14%) 정상 표시 확인

### 2. 실사용 개선 + KIS(한국투자증권) 연동 계획 수립 (Plan mode)
- "삼성증권 API로 자산현황 자동관리" 요청 → 조사 결과 **삼성증권은 개인용 Open API 미제공**(기관
  전용)임을 확인, 개인 신청 가능한 **한국투자증권(KIS) Open API**로 방향 확정(사용자 승인)
- 코드 리뷰 에이전트로 기존 시스템의 실사용 관점 문제 파악: 매매내역 수정/삭제 불가, 매도 시
  실현손익 소실, 배당금이 수익률 계산에 미반영, 다중통화 미처리, 공용 헬퍼 코드 중복 등
- 사용자 확인 사항: KIS 연동은 체결내역을 매매내역 DB에 자동 임포트하는 방식(옵션2)으로, 모의투자
  계좌는 아직 미등록(사전 준비 필요), USD 종목 보유 계획 없음(다중통화는 후순위), 매매내역
  수정/삭제는 전용 UI 필요
- `C:\Users\waaaw\.claude\plans\proud-plotting-token.md`에 최종 계획 작성 후 승인받음

### 3. Part A(기존 시스템 개선) 구현 완료
- **공용 헬퍼**(`notion_helpers.py`): `portfolio.py`/`webapp.py`/`prices.py`에 중복되던 속성 추출
  로직을 하나로 통합
- **입력값 검증**(`validation.py`): 수량/단가 양수, 수수료 0 이상, 날짜 형식 검증. 음수 수량 입력 시
  즉시 에러로 막히는 것을 실제로 확인
- **매매내역 수정/삭제**: `transactions.py`에 `update_transaction`/`delete_transaction`/
  `find_duplicate_transaction` 추가. CLI(`list-transactions`/`edit-transaction`/
  `delete-transaction`)와 웹 UI(`/transactions/<code>` 페이지, 인라인 수정/삭제 폼) 양쪽 지원.
  수정/삭제 시 해당 종목만 재계산(전체 재계산 대신)
- **매수/매도 상수화**: `transactions.BUY`/`SELL`/`TRADE_TYPES`로 매직스트링 정리
- **실현손익 추적**: `portfolio._replay_transactions`가 보유수량/평균단가 계산과 매도 건별
  실현손익(`RealizedGain`)을 동시에 계산하도록 리팩터링. `compute_holding`은 기존 시그니처 유지해
  기존 6개 테스트 그대로 통과, `compute_realized_gains`용 신규 테스트 3개 추가(총 9개 통과)
- **배당 포함 총수익**: `dividends.total_dividends_for_stock` 추가, 포트폴리오 요약 DB에
  `누적실현손익`/`총수익(배당포함)`/`총수익률(%)` 속성을 첫 동기화 시 자동 추가
  (`notion_api.ensure_property`)하고 계산해 기록
- 실제 워크스페이스에서 종합 검증: 매도 3주 테스트 → 실현손익 60,000원 계산 정확성 직접 검산 확인
  (평가손익 1,676,500 + 실현손익 60,000 + 배당 4,230 = 총수익 1,740,730 정확히 일치) → 수정/삭제로
  원상복귀 → 중복 입력 경고 동작 확인 → 웹 UI `/transactions/005930` 페이지에서 curl로 수정 라우팅
  검증 완료

## 현재 상태 요약
- Part A 전체 구현 및 실제 Notion 워크스페이스 대상 검증 완료 (단위테스트 9개 통과)
- 포트폴리오 요약 DB에 속성 3개(누적실현손익/총수익(배당포함)/총수익률(%))가 자동 추가된 상태
- 웹 UI에 매매내역 조회/수정/삭제 페이지 추가됨 (`http://127.0.0.1:5000/transactions/<코드>`)

## 다음에 이어서 할 작업 (Part B — KIS 연동, 계획서 참고)
사전 준비 필요(사용자 액션):
1. KIS 증권 계좌에서 모의투자(paper trading) 신청
2. https://apiportal.koreainvestment.com/apiservice 에서 Open API 신청 → appkey/appsecret 발급
3. 실계좌 계좌번호 확보

이후 마일스톤:
1. `kis_api.py` 신규 작성(인증/토큰 캐싱/재시도) + 잔고조회 → 기존 계산과 교차검증(읽기전용)
2. 체결내역 자동 임포트(중복방지 포함) → 매매내역 DB에 자동 기록, 기존 파이프라인 그대로 재사용
3. (스트레치) WebSocket 실시간 체결 push — 상시 프로세스 필요, 별도 검토

상세 설계는 `C:\Users\waaaw\.claude\plans\proud-plotting-token.md` 참고.
